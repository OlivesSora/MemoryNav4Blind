# MemoryNav 实施清单

本清单由 `MemoryNav.md` 和当前 `utils/imu.py`、`utils/gps.py`、
`utils/glasses_camera.py`、`utils/voice.py` 的实际接口整理而来。完成标记必须以代码、
自动化测试或实机记录为证据，不能只以文件已创建为依据。

## P0：可离线验证的数据基础

- [x] 建立 `memory_nav` Python 包、统一 YAML 配置和命令行约定。
- [x] 定义 GPS、IMU、参考轨迹点、锚点和匹配结果数据模型。
- [x] 实现路线目录创建、manifest 状态流转和线程安全 JSONL 会话写入。
- [x] 实现异常退出后可读取的追加式原始数据格式。
- [x] 实现 GCJ-02 经纬度与局部 East/North 米制坐标互转。
- [x] 为以上模块补齐单元测试。

## P1：参考轨迹生成

- [x] 读取并校验 `raw/gps.jsonl` 和可选 `raw/imu.jsonl`。
- [x] 剔除乱序、重复、过期、跳点和不可能速度样本。
- [x] 按质量来源加权平滑 GPS 轨迹，通过锚点事件保护首尾及指定采样点。
- [x] 按弧长等距重采样，计算累计里程、融合航向和曲率。
- [x] 生成 `reference_trajectory.json`、`quality_report.json` 并更新 manifest。
- [x] 提供 `build_reference.sh` 和合成数据端到端测试。

## P2：在线重演核心

- [x] 实现带前后进度窗口的最近线段投影匹配。
- [x] 计算带符号横向偏移、环形偏航误差和匹配质量。
- [x] 防止平行路段、交叉点和折返路线发生远距离跳段。
- [x] 实现正常、降级、偏离、严重偏离、恢复和定位丢失状态机。
- [x] 提供离线重放、SVG 轨迹可视化入口及合成路线端到端测试。

## P3：锚点与语音核心

- [x] 根据起终点、转角和最大间距产生锚点候选。
- [x] 实现图片清晰度检测和锚点 JSON 持久化。
- [x] 实现提前、到点、偏离、恢复提示的去重与冷却调度。
- [x] 实现 XFeat 锚点匹配适配层和几何内点校验。
- [x] 为 TTS 播放失败提供非阻塞降级测试；无相机实机降级仍在 P4 验收。
- [x] 将锚点推进、偏离/恢复提示和非阻塞 TTS 接入在线 `ReplayRunner`，并用伪传感器完成链路测试。
- [x] 将锚点窗口内的相机采集与 XFeat 校验接入在线重演，并测试失败时继续使用 GPS+IMU。
- [x] 将路线录制期间的起点、转弯、固定间距和终点候选图片实际写入会话，并关联到生成后的锚点。
- [x] 提供锚点候选查看、确认提示文本和拒绝删除的人工审核 CLI。

## P4：硬件采集与联合标定

- [x] 为 IMU 增加逐帧有界缓冲、排空接口和缓冲溢出诊断；仍需实机统计串口及缓冲丢帧率。
- [x] 扩展 GPS 服务返回定位时间、来源、精度、fix quality 和样本年龄，并保持旧客户端接口兼容。
- [x] 为眼镜相机帧增加主机单调时钟元数据；设备端未提供原始时间戳，需在眼镜协议支持后补充。
- [x] 完成录制前传感器检查、有界后台落盘和 IMU 缓冲排空；仍需实机压测队列容量。
- [x] 完成 AprilGrid 原始数据及 Kalibr `cam0/imu0.csv` 数据集采集、bag creator/求解命令、结果导入和标定 YAML 质量校验；仍需安装 Kalibr 并用真实数据求解。
- [x] 编写 `start_calibration.sh`、`start_recording.sh`、`start_replay.sh`。
- [x] 基础设备冒烟验证：眼镜相机持续收帧（约 0.10 s 间隔）、GPS 返回 `good`、N100 IMU 航向稳定（用户实机日志，2026-08-29）。
- [x] 提供 `hardware_acceptance.sh`，支持实机冒烟与 JSONL 状态顺序验收，并原子累计 JSON 质量报告。
- [ ] 实机记录 RTK → 手机 → RTK 定位来源切换并通过顺序验收。
- [ ] 实机记录 GPS/相机断流及恢复，确认重演不中断且状态正确降级。
- [ ] 实机沿参考路线完成正常 → 偏离 → 恢复 → 正常的联合重演验收。

## 完成门槛

- [x] `python3 -m unittest discover -s memory_nav/tests -v` 全部通过。
- [x] 所有 Bash 入口可从任意当前目录解析并显示帮助，错误参数返回非零状态。
- [x] 新增业务代码只存在于 `memory_nav/`；对 `utils/` 的修改仅为兼容的传感器元数据/缓冲接口，并保留用户已有配置变更。
- [ ] 上述三个联合硬件场景均有实机日志，且累计质量报告全部通过。
