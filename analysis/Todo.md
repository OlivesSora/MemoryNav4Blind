# Todo

## 待办

- [ ] 播报方向指令时删除末尾的"X米"
  - 现状：方向指令形如 `向前直行8米`、`向3点钟方向前进8米`。
  - 目标：所有输出的指令文本（含 JSONL、HTML 展示、语音播报）均去掉末尾的"X米"，只保留动作：`向前直行`、`向3点钟方向前进`。
  - 涉及位置：`memory_nav/analysis/core.py` 的 `make_command()` / `format_suggested_direction()`（及建议方向、1Hz 时钟指令的生成）。
  - 注意：同步更新 JSONL 字段（`command` / `suggested_direction`）与 HTML 表格展示，保持一致。

## 其它待办

- [ ] 提高图像文字识别准确性：视觉大模型对锚点原图的文字识别存在漏检 / 错字 / 误识别，不能直接作为可靠地名导航依据。
- [ ] 清理参考轨迹闭环：录制时避免绕圈，或预处理时自动检测并剔除闭环，从根本上解决匹配卡死问题。
- [ ] 提升跟随精度验证：用同路径、相近视角的两次重合行走重测，验证 good 占比与横向误差能否显著改善。
- [ ] 帧↔GPS 对齐鲁棒性：当 GPS 日志缺失 `image_path` 帧号标定时，目前的帧率估算会降低对齐精度，需要备用对齐方案。
- [ ] 视觉相似度对视角敏感：两次行走头部朝向不同会降低同地点相似度，考虑用旋转鲁棒的特征或局部特征匹配替代/补充全局嵌入。
