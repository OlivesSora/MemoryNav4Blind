# MemoryNav × VINS-Fusion 串联计划（2026-09-03）

> 目的：把 VINS-Fusion（稠密、平滑、IMU 度量的 VIO 里程计）接入 MemoryNav 的
> 「录制 → 重建 → 跟踪」闭环，实现 **记录原始轨迹 → 重建高精度参考轨迹 →
> 下次跑动跟踪该参考轨迹**，并在录制中自动加入**关键视觉锚点**。

## 1. 当前目的（为什么串联）

- MemoryNav 目前只依赖 **1 Hz、有噪声的 GPS** 定位重建与跟踪参考轨迹：
  离线测试中横向误差中位数约 **13 m**，94 个测试点 **77 个被判 `lost`**。
  位置不连续、重建稀疏，跟随能力弱。
- VINS-Fusion（眼镜相机 + N100 IMU，单目 VIO）输出 **高频、平滑、IMU 度量的
  `/odometry`**（本地 `world` 系，可 `--loop` 回环），恰好补足
  GPS 的稀疏与跳变；短-中程可到亚米级。
- 目标闭环：

  ```text
  录制(记录原始轨迹)  →  重建(VINS+GPS 对齐生成密集参考轨迹)
        ↓                                       ↓
  录制中关键视觉锚点                       在线跟踪(投影到参考轨迹)
  ```

## 2. 核心难点：跨会话坐标系对齐

VINS 的 `world` 系**每次运行都是任意原点/偏航**（锚定首帧）；MemoryNav 参考轨迹
在 **GPS 局部东-北(EN)米制系**（GCJ-02 原点）。因此：

- **重建**：把 VINS 轨迹按时间戳对齐到 GPS-EN（单目+IMU 尺度≈1 → 刚性 SE(2)，
  Umeyama/Horn 估计旋转+平移）。
- **在线跟踪**：下次跑时 live VINS 的 world 系又变了 → 需在线估计 VINS→参考系
  变换（GPS 强时用 GPS 对齐；GPS 弱时用视觉重定位兜底）。

## 3. 已确认决策（2026-09-03）

- **VINS 接入方式**：旁路桥（UDP/文件转发 `/odometry`），
  让 MemoryNav 不依赖 ROS2/rclpy 环境；桥独立进程，负责转发+时间戳。
- **本轮范围**：先做「录制 + 重建」；**在线跨会话跟踪是下一轮**。
- **数据路径统一**：以 `routes/<id>/raw/` 为单一数据源，重构 `analysis/` 消费它，
  消除目前 recorder 与 analysis 走两套 `output.jsonl` 的分叉。

## 4. 要做的内容

### P0 数据打通（本轮）
- [ ] 定义 `VIOPose` 数据模型（monotonic_ns、x/y/z、四元数、速度、source、质量）。
- [ ] 写 VINS→旁路转发器：订阅 `/odometry`（及可选 `/path`），
  UDP/文件推送给 recorder，打主机单调时钟；确认丢包与时间戳。
- [ ] 扩展 recorder：录制时同步写入 `routes/<id>/raw/vio.jsonl` 与对齐帧
  `routes/<id>/frames/`（真单调时间戳，替代 analysis 现用 fps+start 近似）。
- [ ] 把 `analysis/` 输入统一改为读 route 的 `raw/gps.jsonl`+`raw/vio.jsonl`+帧，
  删除对外部 `output.jsonl` 的依赖；补迁移测试。

### P1 轨迹重建（本轮）
- [ ] 实现 SE(2) 轨迹对齐（Umeyama/Horn，尺度≈1）：VINS world → GPS-EN。
- [ ] 时间戳对齐：估计 VINS 时钟与 recorder 单调钟的映射，配对对应位姿。
- [ ] 生成**高密度平滑参考轨迹**：VINS 为骨架、GPS 做全局校正、IMU 航向约束。
- [ ] 把对齐参数（旋转/平移/原点/时间偏移）写入 `manifest.json`。
- [ ] `quality_report.json` 增加 VINS 漂移量、对齐残差、覆盖比例。

### P2 在线跟踪（下一轮）
- [ ] live VINS + GPS → 在线估计 VINS→参考系变换，接入 `RouteMatcher`。
- [ ] GPS 弱/VIO lost 时用视觉重定位（DINOv2 锚点嵌入 / VINS keyframe）放回参考系。
- [ ] 状态机扩展：区分 VIO lost / GPS lost / 真偏离。

### P3 视觉锚点（随重建推进）
- [ ] 录制中自动采 VINS keyframe + 转弯/间距候选为锚点。
- [ ] 用 VINS 回环增强锚点（回环处确认，降低重复场景误匹配）。
- [ ] 锚点统一走 XFeat/DINOv2，并关联 VINS keyframe id。

## 5. 相比现状的改进

| 现状 | 改进后 |
| --- | --- |
| GPS 1Hz 稀疏、噪声（横向误差中位 13 m） | VINS 高频平滑，短中程亚米级 |
| 重建为稀疏 GPS 折线 | 密集平滑的 VINS+GPS 融合轨迹 |
| recorder 与 analysis 两套数据格式 | 统一 `routes/<id>/raw/` 单一数据源 |
| 帧对齐用 fps+start 近似 | 真单调时间戳对齐 |
| 跨会话定位仅靠 GPS | VIO 连续定位 + 视觉重定位兜底 |
| 锚点来源单一 | VINS keyframe + 回环增强 |

## 6. 验收（本轮）
- [ ] recorder 录制产物含 `raw/vio.jsonl` + 同步帧，且 `analysis/` 可直接消费。
- [ ] 同一条路线离线重建：对齐残差与平滑度指标写进质量报告。
- [ ] 单元/集成测试通过：`python3 -m unittest discover -s memory_nav/tests -v`。
- [ ] 实机一次往返：记录 VINS+GPS，重建出与 GPS 走向一致的密集参考轨迹。